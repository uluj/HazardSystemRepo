/// <summary>
/// DataManagerWindow tarafından bulunabilmesi için 
/// tüm veri ScriptableObject'lerinin uygulaması gereken boş arayüz (etiket).
/// </summary>
public interface ISystemData
{
    // Bu arayüzün boş kalması amaçlanmıştır.
    // Sadece tipleri (types) etiketlemek için kullanılır.
}